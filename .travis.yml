cache:
  directories:
    - node_modules
install:
  - 'npm install'
language: node_js
node_js:
  - stable
  - node
script:
  - 'npm run test'
  - npm run coveralls
services:
  - postgresql
before_script:
  - psql -c 'create database test;' -U postgres
